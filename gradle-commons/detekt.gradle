configurations {
    detekt
}

dependencies {
    detekt 'io.gitlab.arturbosch.detekt:detekt-cli:1.6.0'
}

task detekt(type: JavaExec, group: "verification") {
    main = "io.gitlab.arturbosch.detekt.cli.Main"
    classpath = configurations.detekt
    ignoreExitValue = true
    def input = "$projectDir"
    def config = "$rootDir/android-commons/gradle-commons/detekt-config.yml"
    def report = "xml:$reportsDir/detekt/detekt.xml,html:$reportsDir/detekt/detekt.html"
    def params = [ '-i', input, '-c', config, '-r', report]
    args(params)
}